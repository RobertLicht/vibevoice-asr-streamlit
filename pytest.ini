[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
filterwarnings =
    # Suppress the deprecated feature_extractor_class warning from transformers.
    ignore:.*feature_extractor_class.*is deprecated.*:DeprecationWarning
